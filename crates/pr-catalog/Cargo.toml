[package]
name = "pr-catalog"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Penguin Redis — pr-catalog: command metadata, capability and local authority"

[lints]
workspace = true

[dependencies]
pr-core = { path = "../pr-core" }
blake3 = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }   # Penguin-owned catalog snapshot schema only (§8.3)
thiserror = { workspace = true }
