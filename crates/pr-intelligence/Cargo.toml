[package]
name = "pr-intelligence"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
publish = false
description = "Penguin Redis — pr-intelligence: lenient analyser, suggestions, signature help"

[lints]
workspace = true

[dependencies]
pr-core = { path = "../pr-core" }
pr-catalog = { path = "../pr-catalog" }
pr-repl = { path = "../pr-repl" }
thiserror = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
pr-render = { path = "../pr-render" }
# V-F07's live half drives the discovery state machine against a real server.
pr-protocol = { path = "../pr-protocol" }
pr-transport = { path = "../pr-transport" }
bytes = { workspace = true }
toml = { workspace = true }
blake3 = { workspace = true }   # reading the V-F08 corpora in tests only
# V-F09 measures the heap with the same instrument `xtask measure-baseline` uses.
xtask = { path = "../../xtask" }
